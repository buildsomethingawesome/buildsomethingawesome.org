---
project : Build Something Awesome
# The following URLs are the web addresses where the site will be deployed.
# You must have one for your production deployment, and you may have one for your staging deployment.
# It may be a top-level address (http://example.com) or a folder (http://example.com/something)
production_url : http://buildsomethingawesome.org
staging_url : http://demo.avh4.net/bsa
# The following are the destinations that rsync will push to when you use 'rake deploy'
# It will typically be in the form of <server>:<folder>.
# This MUST end with a trailing '/'
production_server : ara:buildsomethingawesome.org/
staging_server : ara:demo.avh4.net/bsa/
